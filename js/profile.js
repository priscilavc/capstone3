function editProfile() {
    // code to open an edit modal or redirect to an edit page
  }
  function logout() {
    // Code to clear user's session data or make a logout request to the server
    window.location.href = '/logout';  // redirecting to logout page
}
